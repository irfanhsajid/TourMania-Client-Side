import React from 'react';

const Packages = () => {
    return (
        <div>
            <h2 className="text-danger text-center">This is Packages section and it should be Private</h2>
        </div>
    );
};

export default Packages;